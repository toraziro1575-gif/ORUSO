#!/usr/bin/env bash
set -euo pipefail

python3 - <<'PY'
from pathlib import Path
import re

path = Path("lib/main.dart")
text = path.read_text(encoding="utf-8")

pattern = r"class WeightPoint\b[\s\S]*?(?=class CalendarPage\b)"
replacement = "\nclass WeightPoint {\n  const WeightPoint(this.date, this.weight);\n  final DateTime date;\n  final double weight;\n}\n\nclass WeightSeries {\n  const WeightSeries({\n    required this.uid,\n    required this.name,\n    required this.points,\n    required this.isMe,\n  });\n\n  final String uid;\n  final String name;\n  final List<WeightPoint> points;\n  final bool isMe;\n}\n\nclass WeightPage extends StatefulWidget {\n  const WeightPage({super.key, required this.uid});\n  final String uid;\n\n  @override\n  State<WeightPage> createState() => _WeightPageState();\n}\n\nclass _WeightPageState extends State<WeightPage> {\n  final controller = TextEditingController();\n  final targetController = TextEditingController();\n\n  @override\n  void dispose() {\n    controller.dispose();\n    targetController.dispose();\n    super.dispose();\n  }\n\n  Future<void> addWeight() async {\n    final value = double.tryParse(controller.text);\n    if (value == null) {\n      ScaffoldMessenger.of(context).showSnackBar(\n        const SnackBar(content: Text('体重を数字で入力してください。')),\n      );\n      return;\n    }\n\n    await records(widget.uid, 'weights').add({\n      'weight': value,\n      'createdAt': FieldValue.serverTimestamp(),\n    });\n    controller.clear();\n  }\n\n  Future<void> saveTarget() async {\n    final value = double.tryParse(targetController.text);\n    if (value == null) {\n      ScaffoldMessenger.of(context).showSnackBar(\n        const SnackBar(content: Text('目標体重を数字で入力してください。')),\n      );\n      return;\n    }\n\n    await FirebaseFirestore.instance\n        .collection('users')\n        .doc(widget.uid)\n        .collection('goals')\n        .doc('current')\n        .set({\n      'targetWeight': value,\n      'updatedAt': FieldValue.serverTimestamp(),\n    }, SetOptions(merge: true));\n    targetController.clear();\n  }\n\n  void openGraph() {\n    Navigator.of(context).push(\n      MaterialPageRoute(\n        builder: (_) => WeightGraphHubPage(uid: widget.uid),\n      ),\n    );\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    final goalRef = FirebaseFirestore.instance\n        .collection('users')\n        .doc(widget.uid)\n        .collection('goals')\n        .doc('current');\n\n    return StreamBuilder<DocumentSnapshot<Map<String, dynamic>>>(\n      stream: goalRef.snapshots(),\n      builder: (context, goalSnapshot) {\n        final goal =\n            (goalSnapshot.data?.data()?['targetWeight'] as num?)?.toDouble();\n\n        return StreamBuilder<QuerySnapshot<Map<String, dynamic>>>(\n          stream: records(widget.uid, 'weights')\n              .orderBy('createdAt', descending: true)\n              .snapshots(),\n          builder: (context, snapshot) {\n            final docs = snapshot.data?.docs ?? [];\n            final latest = docs.isEmpty\n                ? null\n                : (docs.first.data()['weight'] as num).toDouble();\n\n            return ListView(\n              padding: const EdgeInsets.all(16),\n              children: [\n                const SectionTitle('体重を記録'),\n                TextField(\n                  controller: controller,\n                  keyboardType:\n                      const TextInputType.numberWithOptions(decimal: true),\n                  decoration: const InputDecoration(\n                    labelText: '体重（kg）',\n                    border: OutlineInputBorder(),\n                  ),\n                ),\n                const SizedBox(height: 12),\n                FilledButton.icon(\n                  onPressed: addWeight,\n                  icon: const Icon(Icons.add),\n                  label: const Text('体重を追加'),\n                ),\n                const SizedBox(height: 18),\n                Card(\n                  child: Padding(\n                    padding: const EdgeInsets.all(16),\n                    child: Column(\n                      crossAxisAlignment: CrossAxisAlignment.stretch,\n                      children: [\n                        Row(\n                          children: [\n                            Expanded(\n                              child: StatCard(\n                                icon: Icons.monitor_weight,\n                                value: latest == null\n                                    ? '未記録'\n                                    : '${latest.toStringAsFixed(1)} kg',\n                                label: '現在体重',\n                              ),\n                            ),\n                            const SizedBox(width: 8),\n                            Expanded(\n                              child: StatCard(\n                                icon: Icons.flag,\n                                value: goal == null\n                                    ? '未設定'\n                                    : '${goal.toStringAsFixed(1)} kg',\n                                label: '目標体重',\n                              ),\n                            ),\n                          ],\n                        ),\n                        const SizedBox(height: 12),\n                        Row(\n                          children: [\n                            Expanded(\n                              child: TextField(\n                                controller: targetController,\n                                keyboardType:\n                                    const TextInputType.numberWithOptions(\n                                  decimal: true,\n                                ),\n                                decoration: const InputDecoration(\n                                  labelText: '目標体重（kg）',\n                                  border: OutlineInputBorder(),\n                                ),\n                              ),\n                            ),\n                            const SizedBox(width: 8),\n                            FilledButton(\n                              onPressed: saveTarget,\n                              child: const Text('設定'),\n                            ),\n                          ],\n                        ),\n                      ],\n                    ),\n                  ),\n                ),\n                const SizedBox(height: 18),\n                FilledButton.tonalIcon(\n                  onPressed: openGraph,\n                  icon: const Icon(Icons.show_chart, size: 28),\n                  label: const Padding(\n                    padding: EdgeInsets.symmetric(vertical: 14),\n                    child: Text(\n                      '体重グラフを大きく見る',\n                      style: TextStyle(fontSize: 18),\n                    ),\n                  ),\n                ),\n                const SizedBox(height: 22),\n                const SectionTitle('体重記録'),\n                if (docs.isEmpty)\n                  const EmptyCard(\n                    icon: Icons.monitor_weight,\n                    title: '記録はありません',\n                    subtitle: '最初の体重を追加しましょう。',\n                  )\n                else\n                  ...docs.map((doc) {\n                    final data = doc.data();\n                    final dt = dateFrom(data['createdAt']);\n                    return RecordTile(\n                      icon: Icons.monitor_weight,\n                      title:\n                          '${(data['weight'] as num).toStringAsFixed(1)} kg',\n                      subtitle: '${ymd(dt)} ${hm(dt)}',\n                      trailing: IconButton(\n                        icon: const Icon(Icons.delete_outline),\n                        onPressed: doc.reference.delete,\n                      ),\n                    );\n                  }),\n              ],\n            );\n          },\n        );\n      },\n    );\n  }\n}\n\nclass WeightGraphHubPage extends StatefulWidget {\n  const WeightGraphHubPage({super.key, required this.uid});\n  final String uid;\n\n  @override\n  State<WeightGraphHubPage> createState() => _WeightGraphHubPageState();\n}\n\nclass _WeightGraphHubPageState extends State<WeightGraphHubPage> {\n  String period = '1か月';\n  final Set<String> selectedFriendUids = {};\n  bool friendPanelOpen = true;\n\n  List<WeightPoint> filterPoints(List<WeightPoint> all) {\n    final now = DateTime.now();\n    DateTime? start;\n    if (period == '1週間') {\n      start = now.subtract(const Duration(days: 7));\n    } else if (period == '1か月') {\n      start = DateTime(now.year, now.month - 1, now.day);\n    } else if (period == '3か月') {\n      start = DateTime(now.year, now.month - 3, now.day);\n    }\n\n    if (start == null) {\n      return all;\n    }\n    return all.where((point) => point.date.isAfter(start!)).toList();\n  }\n\n  Future<List<Map<String, dynamic>>> loadFriends() async {\n    final friendships = await FirebaseFirestore.instance\n        .collection('friendships')\n        .where('members', arrayContains: widget.uid)\n        .get();\n\n    final result = <Map<String, dynamic>>[];\n    for (final doc in friendships.docs) {\n      final members = List<String>.from(doc.data()['members'] as List);\n      final friendUid = members.firstWhere((uid) => uid != widget.uid);\n      final profile = await FirebaseFirestore.instance\n          .collection('users')\n          .doc(friendUid)\n          .get();\n      final data = profile.data();\n      if (data != null &&\n          (data['weightGraphVisibility'] as String? ?? 'private') ==\n              'friends') {\n        result.add({\n          'uid': friendUid,\n          'nickname': data['nickname'] as String? ?? 'ORUSOユーザー',\n          'avatarKey': data['avatarKey'] as String? ?? 'mint',\n        });\n      }\n    }\n    return result;\n  }\n\n  Future<List<WeightSeries>> loadSelectedFriendSeries(\n    List<Map<String, dynamic>> friends,\n  ) async {\n    final series = <WeightSeries>[];\n\n    for (final friend in friends) {\n      final uid = friend['uid'] as String;\n      if (!selectedFriendUids.contains(uid)) {\n        continue;\n      }\n\n      final weights = await records(uid, 'weights')\n          .orderBy('createdAt')\n          .get();\n\n      final points = weights.docs\n          .map(\n            (doc) => WeightPoint(\n              dateFrom(doc.data()['createdAt']),\n              (doc.data()['weight'] as num).toDouble(),\n            ),\n          )\n          .toList();\n\n      series.add(\n        WeightSeries(\n          uid: uid,\n          name: friend['nickname'] as String,\n          points: filterPoints(points),\n          isMe: false,\n        ),\n      );\n    }\n\n    return series;\n  }\n\n  @override\n  Widget build(BuildContext context) {\n    final goalRef = FirebaseFirestore.instance\n        .collection('users')\n        .doc(widget.uid)\n        .collection('goals')\n        .doc('current');\n\n    return Scaffold(\n      appBar: AppBar(\n        title: const Text('体重グラフ'),\n        actions: [\n          IconButton(\n            tooltip: friendPanelOpen\n                ? 'フレンド選択を閉じる'\n                : 'フレンド選択を開く',\n            onPressed: () =>\n                setState(() => friendPanelOpen = !friendPanelOpen),\n            icon: Icon(\n              friendPanelOpen ? Icons.people : Icons.people_outline,\n            ),\n          ),\n        ],\n      ),\n      body: FutureBuilder<List<Map<String, dynamic>>>(\n        future: loadFriends(),\n        builder: (context, friendsSnapshot) {\n          final friends = friendsSnapshot.data ?? [];\n\n          return StreamBuilder<QuerySnapshot<Map<String, dynamic>>>(\n            stream: records(widget.uid, 'weights')\n                .orderBy('createdAt')\n                .snapshots(),\n            builder: (context, myWeightsSnapshot) {\n              final myPoints = (myWeightsSnapshot.data?.docs ?? [])\n                  .map(\n                    (doc) => WeightPoint(\n                      dateFrom(doc.data()['createdAt']),\n                      (doc.data()['weight'] as num).toDouble(),\n                    ),\n                  )\n                  .toList();\n\n              return StreamBuilder<\n                  DocumentSnapshot<Map<String, dynamic>>>(\n                stream: goalRef.snapshots(),\n                builder: (context, goalSnapshot) {\n                  final goal = (goalSnapshot.data\n                          ?.data()?['targetWeight'] as num?)\n                      ?.toDouble();\n\n                  return FutureBuilder<List<WeightSeries>>(\n                    future: loadSelectedFriendSeries(friends),\n                    builder: (context, seriesSnapshot) {\n                      final allSeries = <WeightSeries>[\n                        WeightSeries(\n                          uid: widget.uid,\n                          name: '自分',\n                          points: filterPoints(myPoints),\n                          isMe: true,\n                        ),\n                        ...(seriesSnapshot.data ?? []),\n                      ];\n\n                      return ListView(\n                        padding: const EdgeInsets.all(16),\n                        children: [\n                          Row(\n                            children: [\n                              Expanded(\n                                child: SegmentedButton<String>(\n                                  segments: const [\n                                    ButtonSegment(\n                                      value: '1週間',\n                                      label: Text('1週'),\n                                    ),\n                                    ButtonSegment(\n                                      value: '1か月',\n                                      label: Text('1か月'),\n                                    ),\n                                    ButtonSegment(\n                                      value: '3か月',\n                                      label: Text('3か月'),\n                                    ),\n                                    ButtonSegment(\n                                      value: '全期間',\n                                      label: Text('全部'),\n                                    ),\n                                  ],\n                                  selected: {period},\n                                  onSelectionChanged: (values) =>\n                                      setState(() => period = values.first),\n                                ),\n                              ),\n                            ],\n                          ),\n                          if (friendPanelOpen) ...[\n                            const SizedBox(height: 14),\n                            Card(\n                              child: Padding(\n                                padding: const EdgeInsets.all(14),\n                                child: Column(\n                                  crossAxisAlignment:\n                                      CrossAxisAlignment.start,\n                                  children: [\n                                    Row(\n                                      children: [\n                                        const Expanded(\n                                          child: Text(\n                                            'グラフに重ねるフレンド',\n                                            style: TextStyle(\n                                              fontWeight: FontWeight.bold,\n                                            ),\n                                          ),\n                                        ),\n                                        if (selectedFriendUids.isNotEmpty)\n                                          TextButton(\n                                            onPressed: () => setState(\n                                              selectedFriendUids.clear,\n                                            ),\n                                            child: const Text('全部消す'),\n                                          ),\n                                      ],\n                                    ),\n                                    const SizedBox(height: 8),\n                                    if (friendsSnapshot.connectionState ==\n                                        ConnectionState.waiting)\n                                      const LinearProgressIndicator()\n                                    else if (friends.isEmpty)\n                                      const Text(\n                                        '体重グラフを公開しているフレンドはいません。',\n                                      )\n                                    else\n                                      Wrap(\n                                        spacing: 8,\n                                        runSpacing: 8,\n                                        children: friends.map((friend) {\n                                          final uid =\n                                              friend['uid'] as String;\n                                          final selected =\n                                              selectedFriendUids.contains(uid);\n                                          return FilterChip(\n                                            selected: selected,\n                                            avatar: OrusoAvatar(\n                                              avatarKey:\n                                                  friend['avatarKey'] as String,\n                                              radius: 14,\n                                            ),\n                                            label: Text(\n                                              friend['nickname'] as String,\n                                            ),\n                                            onSelected: (value) {\n                                              setState(() {\n                                                if (value) {\n                                                  selectedFriendUids.add(uid);\n                                                } else {\n                                                  selectedFriendUids.remove(uid);\n                                                }\n                                              });\n                                            },\n                                          );\n                                        }).toList(),\n                                      ),\n                                  ],\n                                ),\n                              ),\n                            ),\n                          ],\n                          const SizedBox(height: 18),\n                          SizedBox(\n                            height: 500,\n                            child: MultiWeightGraph(\n                              series: allSeries,\n                              targetWeight: goal,\n                            ),\n                          ),\n                          const SizedBox(height: 12),\n                          const Text(\n                            '左右にスライドすると過去の記録を確認できます。'\n                            'フレンド名のチップを押すと表示・非表示を切り替えられます。',\n                            textAlign: TextAlign.center,\n                          ),\n                        ],\n                      );\n                    },\n                  );\n                },\n              );\n            },\n          );\n        },\n      ),\n    );\n  }\n}\n\nclass MultiWeightGraph extends StatelessWidget {\n  const MultiWeightGraph({\n    super.key,\n    required this.series,\n    required this.targetWeight,\n  });\n\n  final List<WeightSeries> series;\n  final double? targetWeight;\n\n  @override\n  Widget build(BuildContext context) {\n    final usable = series.where((item) => item.points.isNotEmpty).toList();\n    if (usable.isEmpty) {\n      return const EmptyCard(\n        icon: Icons.show_chart,\n        title: 'グラフに表示する記録がありません',\n        subtitle: '体重を追加すると推移が表示されます。',\n      );\n    }\n\n    final allValues = usable\n        .expand((item) => item.points)\n        .map((point) => point.weight)\n        .toList();\n\n    if (targetWeight != null) {\n      allValues.add(targetWeight!);\n    }\n\n    var minY = allValues.reduce(min) - 2;\n    var maxY = allValues.reduce(max) + 2;\n    if (maxY - minY < 4) {\n      minY -= 2;\n      maxY += 2;\n    }\n\n    final longest =\n        usable.map((item) => item.points.length).reduce(max);\n    final graphWidth = max(\n      MediaQuery.sizeOf(context).width - 90,\n      longest * 72.0,\n    );\n\n    final palette = <Color>[\n      Theme.of(context).colorScheme.primary,\n      Colors.orange,\n      Colors.purple,\n      Colors.blue,\n      Colors.pink,\n      Colors.teal,\n    ];\n\n    return Card(\n      child: Padding(\n        padding: const EdgeInsets.fromLTRB(8, 16, 8, 10),\n        child: Column(\n          children: [\n            Wrap(\n              alignment: WrapAlignment.center,\n              spacing: 10,\n              runSpacing: 6,\n              children: List.generate(usable.length, (index) {\n                return Row(\n                  mainAxisSize: MainAxisSize.min,\n                  children: [\n                    Container(\n                      width: 16,\n                      height: 4,\n                      color: palette[index % palette.length],\n                    ),\n                    const SizedBox(width: 5),\n                    Text(usable[index].name),\n                  ],\n                );\n              }),\n            ),\n            const SizedBox(height: 14),\n            Expanded(\n              child: Row(\n                children: [\n                  SizedBox(\n                    width: 46,\n                    child: Column(\n                      mainAxisAlignment: MainAxisAlignment.spaceBetween,\n                      children: List.generate(5, (index) {\n                        final value =\n                            maxY - ((maxY - minY) / 4 * index);\n                        return Text(\n                          value.toStringAsFixed(1),\n                          style: Theme.of(context).textTheme.bodySmall,\n                        );\n                      }),\n                    ),\n                  ),\n                  Expanded(\n                    child: SingleChildScrollView(\n                      reverse: true,\n                      scrollDirection: Axis.horizontal,\n                      child: SizedBox(\n                        width: graphWidth,\n                        child: LineChart(\n                          LineChartData(\n                            minX: 0,\n                            maxX: max(1, longest - 1).toDouble(),\n                            minY: minY,\n                            maxY: maxY,\n                            gridData: const FlGridData(show: true),\n                            borderData: FlBorderData(show: true),\n                            titlesData: FlTitlesData(\n                              leftTitles: const AxisTitles(\n                                sideTitles:\n                                    SideTitles(showTitles: false),\n                              ),\n                              topTitles: const AxisTitles(\n                                sideTitles:\n                                    SideTitles(showTitles: false),\n                              ),\n                              rightTitles: const AxisTitles(\n                                sideTitles:\n                                    SideTitles(showTitles: false),\n                              ),\n                              bottomTitles: AxisTitles(\n                                sideTitles: SideTitles(\n                                  showTitles: true,\n                                  reservedSize: 34,\n                                  interval: 1,\n                                  getTitlesWidget: (value, meta) {\n                                    final index = value.round();\n                                    final mine = usable.first.points;\n                                    if (index < 0 ||\n                                        index >= mine.length) {\n                                      return const SizedBox.shrink();\n                                    }\n                                    final date = mine[index].date;\n                                    return Padding(\n                                      padding:\n                                          const EdgeInsets.only(top: 8),\n                                      child: Text(\n                                        '${date.month}/${date.day}',\n                                        style: Theme.of(context)\n                                            .textTheme\n                                            .bodySmall,\n                                      ),\n                                    );\n                                  },\n                                ),\n                              ),\n                            ),\n                            extraLinesData: targetWeight == null\n                                ? const ExtraLinesData()\n                                : ExtraLinesData(\n                                    horizontalLines: [\n                                      HorizontalLine(\n                                        y: targetWeight!,\n                                        dashArray: [6, 4],\n                                        strokeWidth: 2,\n                                        label: HorizontalLineLabel(\n                                          show: true,\n                                          alignment:\n                                              Alignment.topRight,\n                                          labelResolver: (_) =>\n                                              '目標 ${targetWeight!.toStringAsFixed(1)}kg',\n                                        ),\n                                      ),\n                                    ],\n                                  ),\n                            lineTouchData: LineTouchData(\n                              touchTooltipData:\n                                  LineTouchTooltipData(\n                                getTooltipItems: (spots) {\n                                  return spots.map((spot) {\n                                    final seriesIndex =\n                                        spot.barIndex;\n                                    final current =\n                                        usable[seriesIndex];\n                                    final pointIndex =\n                                        spot.x.round();\n                                    if (pointIndex < 0 ||\n                                        pointIndex >=\n                                            current.points.length) {\n                                      return null;\n                                    }\n                                    final point =\n                                        current.points[pointIndex];\n                                    return LineTooltipItem(\n                                      '${current.name}\\n'\n                                      '${ymd(point.date)}\\n'\n                                      '${point.weight.toStringAsFixed(1)} kg',\n                                      const TextStyle(\n                                        fontWeight: FontWeight.bold,\n                                      ),\n                                    );\n                                  }).toList();\n                                },\n                              ),\n                            ),\n                            lineBarsData:\n                                List.generate(usable.length, (index) {\n                              final current = usable[index];\n                              return LineChartBarData(\n                                spots: List.generate(\n                                  current.points.length,\n                                  (pointIndex) => FlSpot(\n                                    pointIndex.toDouble(),\n                                    current\n                                        .points[pointIndex].weight,\n                                  ),\n                                ),\n                                color:\n                                    palette[index % palette.length],\n                                isCurved: false,\n                                barWidth: current.isMe ? 4 : 3,\n                                dotData:\n                                    const FlDotData(show: true),\n                              );\n                            }),\n                          ),\n                        ),\n                      ),\n                    ),\n                  ),\n                ],\n              ),\n            ),\n          ],\n        ),\n      ),\n    );\n  }\n}\n"

updated, count = re.subn(pattern, replacement.strip() + "\n\n", text, count=1)
if count != 1:
    raise SystemExit("体重画面の置換に失敗しました。Ver.0.8適用後に実行してください。")

path.write_text(updated, encoding="utf-8")

pubspec = Path("pubspec.yaml")
pub = pubspec.read_text(encoding="utf-8")
lines = pub.splitlines()
for i, line in enumerate(lines):
    if line.startswith("version:"):
        lines[i] = "version: 0.8.1+9"
        break
pubspec.write_text("\n".join(lines) + "\n", encoding="utf-8")
PY

cat > README.md <<'EOF'
# ORUSO Ver.0.8.1

体重タブは記録入力を中心にし、専用ボタンから大きな体重グラフ画面を開く構成に変更しました。

## 変更内容
- 体重タブに「体重グラフを大きく見る」ボタン
- グラフを専用ページへ分離
- 横スライド式の大きなグラフ
- 自分とフレンドの体重を重ねて表示
- 公開中のフレンドをチップで追加・削除
- 「全部消す」でフレンド表示を一括解除
- 人型ボタンでフレンド選択欄を開閉
- 1週間・1か月・3か月・全期間の切り替え
- 目標体重の点線表示
EOF

git add lib/main.dart pubspec.yaml README.md oruso_v081_weight_graph_hub.sh
git commit -m "Move weight graph to a large friend comparison page" || true
git push origin main

echo
echo "ORUSO Ver.0.8.1 weight graph hub update complete."
